#define ll long long
#include<bits/stdc++.h>

using namespace std;

int main() {
    cout << "Введите x,y";
    ll x, y;
    cin >> x >> y;
    if (x > 0 || y > 0)
    {
        cout << ln
    }
    return 0;
}